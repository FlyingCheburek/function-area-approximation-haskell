module ApproximationRules (
    leftsum,
    rightsum,
    averagesum
) where

leftsum :: Double -> Double
leftsum x = x

rightsum :: Double -> Double
rightsum x = x

averagesum :: Double -> Double
averagesum x = x 